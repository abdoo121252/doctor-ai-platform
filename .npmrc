# Hoist @napi-rs/canvas to the root node_modules so pdfjs-dist's internal
# `require("@napi-rs/canvas")` resolves at runtime (pdfjs-dist has no declared
# dependency on it; it's pulled in for Node canvas rendering).
public-hoist-pattern[]=@napi-rs/canvas